{
  "name": "@fis/mcp",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "oauth:smoke": "node scripts/oauth-agent-smoke.mjs",
    "smoke": "node scripts/agent-smoke.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "agents": "^0.0.98",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250530.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
